<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
  </head>
  <body>
    <br>
    <div class="container">
      <% @list_rn.each do |row| %>
        <h1 class="title is-3"><%= row.nom %></h1>
        <table class="table">
          <thead>
            <tr>
              <th>id</th>
              <th>Nom</th>
              <th>prix</th>
              <th>a</th>
              <th>v</th>
              <th>p</th>
              <th>Qualite prix</th>
            </tr>
          </thead>
          <tbody>
            <% result = VPrestataire.tri_prestataires(row.id) %>
            <% result.each do |item| %>
              <tr>
                <td><%= item["id"] %></td>
                <td><%= item["nom"] %></td>
                <td><%= item["prix"] %></td>
                <td><%= item["a"] %></td>
                <td><%= item["v"] %></td>
                <td><%= item["p"] %></td>
                <td><%= item["qualiteprix"] %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <hr>
      <% end %>
    </div>
  </body>
</html>
